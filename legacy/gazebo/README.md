# Gazebo archive

The Gazebo implementation was retired in favor of the deterministic YAML-driven simulator in `rocket_sim`. No active code depends on Gazebo. Historical Gazebo files belong in this directory if they need to be recovered or consulted.
